
#include <stdio.h>
int binary(int arr[],int a,int b)
{
    int f;
    if(f==1)
    {
        printf("result found");
        return 1;
    }
    else
    {
        
    }
}
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    int ar[a];
    for(int i=0;i<a;i++)
    {
        scanf("%d",&ar[i]);
    }
    
    int a=binary(arr,a,b);
    return 0;
}
