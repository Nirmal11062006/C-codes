#include <stdio.h>
#include<math.h>
float main()
{
    int a=4,b=6,c;
    float x=7.1,y,k;
    c=pow(a,b);
    printf("%d\n",c);
    printf("%f\n",y=floor(x));//floor means removes the decibels
    printf("%f\n",k=ceil(x));//it moves to next digit with out desibels
    
    return 0;
}
